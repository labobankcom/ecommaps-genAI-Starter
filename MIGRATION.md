# 🔄 دليل الانتقال السريع للمطورين (Migration Guide)

## انتقالنا كإيكومابس إلى هندسة الـ SDK مجزأة ومتعددة الطبقات (Layered Architecture)

في إصدار هذا القالب، انتقلنا من كتابة استدعاءات الخادم مباشرة في طبقة المشروع (عبر دوال fetch محلية أو طبقات خفية)، إلى استخدام **مجموعة حزم إيكومابس المتطورة**. هذا التحديث يقدم استقراراً عالياً ودعماً تلقائياً لقواعد فك التشفير وخصائص Typescript.

إذا كنت تستخدم نسخة أقدم من قالب المبيعات (أو أي قالب مبني سابقاً يدوياً)، يوضح هذا الدليل كيفية الانتقال إلى البنية المعمارية الموزعة الجديدة. 

### الهيكل السليم للحزم ووظائفها

| اسم الحزمة (Package) | الاستخدام (Scope) | محتويات ومسؤوليات الحزمة |
| :--- | :--- | :--- |
| **`@ecommaps/client`** | الطبقة الأساسية للخادم (Core API SDK) | توفير الاستجابات المطابقة لنوافذ الواجهة البرمجية (API Endpoints + Typed Contracts). |
| **`@ecommaps/storefront-kit`** | منطق تجارة إلكترونية نظيف وموحد | دمج المتغيرات، توحيد هيكل السلة وعرض المنتجات، وتقييم كوبونات الخصم. |
| **`@ecommaps/ai-sales-agent`** | طبقة المبيعات الذكية التفاعلية | بناء مهام الذكاء الاصطناعي (Agent Tools/Runtime) ومكونات المحادثة المعقدة للـ UI. |

---

## 🛠️ كيف تبدأ بالتحديث أو دمج الإصدار الجديد للقالب؟

### 1- إزالة دوال الـ Fetch المحلية القديمة
- أزل جميع واجهات (shims) المتاجر المكتوبة محلياً في `src/lib/ecommaps.ts`.
- جميع الخصائص كعرض الأقسام (`pages`)، المدونات (`blogs`)، وتأكيد الكوبونات (`coupons.validate`) أصبحت متاحة ومغلفة بالكامل تحت النسخة `@ecommaps/client@^1.4.0`. لا تتكرر برمجتها.

### 2- تثبيت الاعتمادات الجديدة لطبقات واجهة العرض والذكاء الاصطناعي
تأكد من تواجد التحديثات في ملف `package.json` الخاص بمتجرك:
```json
"@ecommaps/storefront-kit": "^0.1.0",
"@ecommaps/ai-sales-agent": "^0.1.1" // يحتوي على حلول لتعقيدات "Maximum update depth"
```

### 3- تعديل مصادر الذكاء الاصطناعي وواجهة المستخدم
إذا كنت تستخدم إصداراً قديماً من الشات المدمج بالقالب، فيمكنك الآن:
- سحب ملف الشخصية المبيعاتية الخاص بإيكومابس من خوادم السيرفر `buildSalesSkillProfile` من الاعتماد `@ecommaps/ai-sales-agent/server`. 
- استيراد مسار تعليل الأفكار التفاعلي المخصص واجهة المستخدم `ReasoningBlock` عن طريق الاعتماد المخصص للريسبونس المباشر `@ecommaps/ai-sales-agent/react`.

### 4- التوافقية المستقرة (No Breaking Changes)
- استخدام `ecommapsClient` الموجود في القالب الحالي متوافق عكسياً مع أي حزم تم إضافتها أو استعلامات سابقة للماضي.
- لا توجد تعديلات (Breaking Changes) تتطلب تغيير جذري في استيراد أحمال الـ Storefront API.
- تظل مسارات الدردشة المعرفة في الواجهة البرمجية الخاصة بـ API `route.ts` قابلة للعمل بدون أي تعطيل.

وبهذا تكون بيئة عملك الحديثة مبنية بشكل نظيف ومستقبلي!
